{
	"id": "refresh-impl",
	"repo": "self",
	"kind": "comprehension",
	"prompt": "When a user runs `codegraph refresh`, does it do a full rebuild or an incremental update, and which function implements the behavior?",
	"rubric": {
		"expectSymbols": ["incremental", "incrementalUpdate"],
		"expectFiles": ["src/ingest/incremental.ts"],
		"forbid": ["does a full rebuild", "performs a full rebuild", "rebuilds the entire graph"]
	}
}
